services:
  labby:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: labby
    ports:
      - "8080:80"
    environment:
      - DATA_DIR=/data
    volumes:
      - labby-data:/data
    restart: unless-stopped

volumes:
  labby-data:
    driver: local
